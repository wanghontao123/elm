### Buttons

``` javascript
     <button
        className='components_buttons'
        style={{ background: color }}  
        onClick={onClick}
    >
        {title}
    </button>

    <Buttons 
        title='退出登录'  // title
        color='#d8584a'  // 背景颜色
        onClick={this.exit}  // 点击事件
    />

```